% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ram.R
\name{ram.new_object}
\alias{ram.new_object}
\title{Add an Unnamed Object to RAM$objects}
\usage{
ram.new_object(RAM, object)
}
\arguments{
\item{RAM}{\href{ram.init}{RAM} object.}

\item{object}{Game object; a list with properties like \verb{$x} and \verb{$spritename}.}
}
\description{
Puts \code{object} into \code{RAM$objects} without assigning it a name. This is useful when iteratively creating objects that will not be referenced by name--- see the \code{Snake} vignette for an example of this.

Otherwise, objects should be put in \code{RAM$objects} like so for easy reference:

\if{html}{\out{<div class="sourceCode">}}\preformatted{RAM$objects$my_object = list(...)
print(RAM$objects$my_object)
}\if{html}{\out{</div>}}
}
\details{
Convenience function for \code{RAM$objects = c(RAM$objects, list(object))}. This is because the syntax for adding a nested list to a list is \code{c(listA, list(listB))}!

Nested lists in R can be confusing, so it's better to have a function that does this automatically than to trust users to remember the correct syntax.
}
