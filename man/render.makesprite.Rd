% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render.makesprite}
\alias{render.makesprite}
\title{Generate Sprite Matrix from Text}
\usage{
render.makesprite(
  txt,
  width = NULL,
  default = 1,
  lookup = c(` ` = 0, . = 0, `_` = 2)
)
}
\arguments{
\item{txt}{String to convert to matrix.}

\item{width}{Sprite width. This is usually set automatically.}

\item{default}{Number to which all characters not in \code{lookup} will be converted.}

\item{lookup}{Table for converting characters to non-\code{default} numbers.}
}
\value{
Returns a sprite matrix suitable for \code{\link[=render.matrix]{render.matrix()}}.
}
\description{
The engine needs sprites to be a matrix of numbers, but this is inconvenient to do by hand. This function converts a string into a sprite to allow the dev to easily create sprites.

By default, all non-space characters inputted will be converted to \code{1} (black) pixels in the sprite, and space characters to \code{0} (transparent).
}
\section{Defaults}{

The height of the sprite matrix is equal to the number of newlines in \code{txt}, as one might expect.
The width is determined by the rightmost non-space character, unless set manually.

Characters in \code{txt} are converted 1-1 with numbers defined by \code{default} and \code{lookup}. The default character conversion is:\tabular{lll}{
    \tab  \tab  \cr
   \verb{        }\code{' '}: \tab \verb{ } \tab \code{0} (transparent) \cr
    \tab  \tab  \cr
   \verb{        }\code{'.'}: \tab  \tab \code{0} (transparent) \cr
    \tab  \tab  \cr
   \verb{        }\code{'_'}: \tab  \tab \code{2} (white) \cr
    \tab  \tab  \cr
   everything else: \tab  \tab \code{1} (black) \cr
}
}

\examples{
smiley = render.makesprite('
  o o
  o o
o     o
 ooooo
')

render.matrix(smiley)

#multicolor
palette =
badger = render.makesprite('
  O O
=     =
 HHHHH
', lookup = c(' ' = 0, 'O' = 1, '=' = 2, 'H' = 3))

print(badger)
render.matrix(badger, palette = c(' ', 'O', 'H', '='))
}
