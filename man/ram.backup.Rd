% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ram.R
\name{ram.backup}
\alias{ram.backup}
\title{Backup RAM}
\usage{
ram.backup(RAM)
}
\arguments{
\item{RAM}{\href{ram.init}{RAM} object.}
}
\value{
RAM, minus \verb{$ROM, $inputs, $debug, $intermediate, $backup,} and \verb{$paused}.\cr This output is stored in the main RAM's \verb{$intermediate} and \verb{$backup}.
}
\description{
Creates a backup of RAM. This is different from a full copy (e.g. \code{my_copy = RAM}):\tabular{lll}{
    \tab  \tab  \cr
   Copy \tab \verb{  } \tab A full copy of the RAM; this will restore the full state of the RAM, deleting any inputs or debug info that may have happened since. \cr
    \tab  \tab  \cr
   Backup \tab  \tab A backup of most RAM data, but excluding inputs and debug information; these will be preserved if the backup is restored. \cr
}
}
\details{
The game uses this to periodically back up the gamestate so that it can restore it in a \link[=ram.rollback]{rollback}.
}
