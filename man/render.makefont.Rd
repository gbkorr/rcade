% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{render.makefont}
\alias{render.makefont}
\title{Fonts}
\usage{
render.makefont(char_group = "uppercase", width, txt)
}
\arguments{
\item{char_group}{Character group to make sprites for; see below.}

\item{width}{Desired width for each character.}

\item{txt}{String to turn into fonts. All nonspace characters will be converted to \code{1} in the sprites.}
}
\description{
Fonts are used to draw text ingame with \code{\link[=render.text]{render.text()}}. A font is a list including \verb{$sprites}, a list of sprites for each character supported by the font.

\link{really try/consider adding random demo photos all over the vignettes: they (esp. inputs and render) are way too dense. even in render you could replace some console examples with images}
All fonts are monospaced.
}
\details{
Fonts can have the following metadata:\tabular{lll}{
    \tab  \tab  \cr
   \verb{$width}: \tab \verb{ } \tab Width of each character in the font, in pixels. \cr
    \tab  \tab  \cr
   \verb{$height}: \tab  \tab Height of each character in the font. \cr
    \tab  \tab  \cr
   \verb{$kerning}: \tab  \tab Default spacing between characters. \cr
    \tab  \tab  \cr
   \verb{$linespacing}: \tab  \tab Default vertical spacing between lines of text. \cr
    \tab  \tab  \cr
   \verb{$sprites}: \tab  \tab List of sprite matrices for each character. \cr
}
}
\section{render.makefont()}{

\code{render.makefont()} is a convenient extension of \code{\link[=render.makesprite]{render.makesprite()}} to create batches of characters at once. To use it, enter a string of all characters in the \code{char_group} lined up horizontally (see examples).

The function does NOT create a full font object; it just generates some of the \verb{$sprites}.

The function returns a list with a sprite set for each character. The sprites' widths must be set by \code{width}, while the height is determined by the number of newlines in \code{txt} like in \code{render.makesprite()}.

The \code{char_group}s are as follows:\tabular{lll}{
    \tab  \tab  \cr
   \code{uppercase}: \tab \verb{  } \tab \code{abcdefghijklmnopqrstuvwxyz} \cr
    \tab  \tab  \cr
   \code{lowercase}: \tab  \tab \code{ABCDEFGHIJKLMNOPQRSTUVWXYZ} \cr
    \tab  \tab  \cr
   \code{numbers}: \tab  \tab \code{0123456789} \cr
    \tab  \tab  \cr
   \code{symbols}: \tab  \tab \verb{()[]\{\}<>+-*/=~.,:;'"}\verb{`}`!?@#$\%^&_ \cr
}


Missing characters will be replaced with empty sprites of the appropriate size.
}

\section{Preinstalled Fonts}{

The package comes with two fonts: \link{fonts.3x3} and the more detailed \link{fonts.3x5}.
}

\examples{
#used for `fonts.3x3`:
example_font = list(
  width = 3,
  height = 3,
  sprites = render.makefont('uppercase',width=3,'
 o  oo  ooo oo  ooo ooo  o  o o ooo ooo o o o   o o ooo ooo ooo  o  ooo  oo ooo o o o o o o o o o o oo
ooo ooo o   o o oo  oo  o   ooo  o   o  oo  o   ooo o o o o ooo o o oo   o   o  o o o o ooo  o   o   o
o o ooo ooo oo  ooo o   ooo o o ooo oo  o o ooo ooo o o ooo o    oo o o oo   o  ooo  o  ooo o o  o   oo
')
)

render.matrix(cbind(
  example_font$sprites$H,
  matrix(0,3,1),
  example_font$sprites$I
))
}
