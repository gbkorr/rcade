% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{render.text}
\alias{render.text}
\title{Render Text as Sprite}
\usage{
render.text(
  str,
  font = rcade::fonts.3x3,
  wrap = FALSE,
  kerning = NULL,
  linespacing = NULL,
  alignment = "left"
)
}
\arguments{
\item{str}{String to make a sprite for.}

\item{font}{\link[=render.makefont]{Font} to render the text in.}

\item{wrap}{Pixels of space alotted to a given line of text before a newline is automatically created. \code{NULL}: no text wrapping.}

\item{kerning}{Pixels between characters horizontally. Otherwise the font's default \verb{$kerning} will be used.}

\item{linespacing}{Pixels between characters vertically. Otherwise the font's default \verb{$linespacing} will be used.}

\item{alignment}{\code{'left'}, \code{'center'}, or \code{'right'} text alignment.}
}
\value{
Returns a sprite matrix for the text.
}
\description{
Generates a sprite for a block of text, suitable for drawing with \link{render.matrix}.
}
\details{
Characters missing from the font will be replaced with blank spaces. If the font only supports uppercase or lowercase, characters will be coerced to the supported case.
}
\section{Limitations}{

This function was somewhat messily implemented, but it does its job. A rewrite would be appropriate someday.

The function currently only supports the wrapping of whole words; individual words will not be split if they exceed the wrap length.

Wrapping also does not work with strings that manually newline (i.e. with \verb{\\n}).
}

\examples{
sprite = render.text('Hello World.', fonts.3x5)
render.matrix(sprite)

#alignment and wrapping
render.matrix(render.text(
  'this text is aligned to the right',
  wrap = 32,
  alignment = 'right'
))

#kerning and linespacing
render.matrix(render.text(
  'very spaced text',
  kerning = 3
))

#newlines with '\n'
render.matrix(render.text(
  'Newlines:\nAre supported.'
))
}
