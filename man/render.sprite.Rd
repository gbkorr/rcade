% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{render.sprite}
\alias{render.sprite}
\title{Render a Sprite to the Scene}
\usage{
render.sprite(scene, sprite, x, y, layer = 1, palette = NULL)
}
\arguments{
\item{scene}{\link[=render.scene]{Scene} object.}

\item{sprite}{Matrix of nonnegative integers corresponding to the pixels of a small image; see \code{\link[=render.matrix]{render.matrix()}}.}

\item{x}{X coordinate of where the sprite should be drawn on the scene. Specifically, this determines the position of top-left pixel of the sprite.}

\item{y}{Y coordinate for the same purpose.}

\item{layer}{Layer on which the sprite should be drawn. Higher layers are drawn on top.}

\item{palette}{Vector to swap the colors (numbers) of the sprite around, e.g. \code{c(0,2,1)} swaps values of 2 and 1. Index starts at 0. Defaults to no swapping.}
}
\value{
Returns the \link[=render.scene]{scene} object with sprite drawn.
}
\description{
Overlays a sprite onto the desired layer of the \code{\link[=render.scene]{scene}} using \code{\link[=render.overlay]{render.overlay()}}.
}
\examples{
I quite dislike this example sir
scene = list(width=24, height=6)
smiley = matrix(c(0,0,1,0,0,0,0,1,1,1,0,1,0,0,0,1,1,1,0,1,0,0,0,1,0,0,1,0), ncol = 7)
box = matrix(1,12,9)
scene = render.sprite(scene, smiley, x=2, y=2)
scene = render.sprite(scene, smiley, x=11, y=2, layer = 2, palette = c(0,2,1)) #reversed colors
scene = render.sprite(scene, box, x=10, y=1)
render.scene(scene)
}
