% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene.R
\name{render.object}
\alias{render.object}
\title{Draw an Object to the Scene}
\usage{
render.object(scene, obj, RAM)
}
\arguments{
\item{scene}{\link[=render.scene]{Scene} object.}

\item{obj}{Object in \code{RAM$objects}. Objects with a \verb{$spritename} will be drawn.}

\item{RAM}{\link[=ram.init]{RAM} object.}
}
\value{
Returns the \link[=render.scene]{scene} object with sprite drawn. This function is called by \code{\link[=render.ram]{render.ram()}} for each object in \code{RAM$objects}.
}
\description{
Calls \code{\link[=render.sprite]{render.sprite()}} using an object's \verb{$spritename} (the name of the desired sprite in \code{RAM$ROM$sprites}).

The object is drawn at position \verb{(obj$x, obj$y)} on layer \code{obj$layer} if specified (see below).

Objects with no \verb{$spritename} will not be drawn, unless they have custom drawing behavior defined in \code{obj$draw()}:

If the object has a function set for \code{obj$draw()}, it will be run instead of \code{render.sprite()}. This allows the game dev to create custom drawing behavior.

\if{html}{\out{<div class="sourceCode">}}\preformatted{scene = obj$draw(scene, obj, RAM)
}\if{html}{\out{</div>}}
}
\details{
An object only needs a \verb{$spritename} to be drawn. Objects can also have the following properties which influence how they're drawn: otherwise, the default value (second column) will be used.\tabular{llll}{
    \tab  \tab  \tab  \cr
   \verb{$spritename} \tab  \tab  \tab Name of sprite in \code{ROM$sprites}. \cr
    \tab  \tab  \tab  \cr
   \verb{$x} \tab \verb{ } \tab \code{1} \tab X-coordinate at which to draw the object's sprite in the scene. \cr
    \tab  \tab  \tab  \cr
   \verb{$y} \tab  \tab \code{1} \tab Y-coordinate. \cr
    \tab  \tab  \tab  \cr
   \verb{$offset.x} \tab  \tab \code{0} \tab Pixels to offset sprite horizontally from obj$x. \cr
    \tab  \tab  \tab  \cr
   \verb{$offset.y} \tab  \tab \code{0} \tab Vertical offset. \cr
    \tab  \tab  \tab  \cr
   \verb{$layer} \tab  \tab \code{2} \tab Layer on which to draw the sprite. See \link{render.scene}; high layers are drawn on top. \cr
    \tab  \tab  \tab  \cr
   \verb{$timer} \tab  \tab \code{RAM$timer}\verb{ } \tab Tick count (ascending) for animations. \cr
    \tab  \tab  \tab  \cr
   \verb{$palette} \tab  \tab \code{NULL} \tab Vector to swap the colors of the object's sprite around, e.g. \code{c(0,2,1)} swaps values of 2 and 1. Index starts at 0. Defaults to no swapping. \cr
    \tab  \tab  \tab  \cr
   \verb{$draw()} \tab  \tab \code{NULL} \tab Overwrites the default drawing behavior for the sprite; see above. \cr
}
}
\section{Offsets}{

Both the \code{obj} and \code{sprite} can have optional \code{offset.x}/\code{offset.y} properties to offset the draw location of their sprite from their position. This can be useful for animation and aligning an object's sprite with its actual hitbox.

If both object and sprite have an offset property, the two are added.
}

\examples{
smileysprite = matrix(c(0,0,1,0,0,0,0,1,1,1,0,1,0,0,0,1,1,1,0,1,0,0,0,1,0,0,1,0), ncol = 7)
RAM = ram.init(rom.init(16,8,sprites=list(smiley=smileysprite)))
RAM$objects$smiley = list(x = 3, y = 3, spritename = 'smiley')
scene = list(width=16,height=8)
scene = render.object(scene,RAM$objects$smiley,RAM)
render.scene(scene)
}
